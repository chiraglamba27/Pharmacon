{
  "name": "pharmacon",
  "version": "1.0.0",
  "private": true,
  "description": "Pharmacon — Prescription Digitisation & Medication Management Platform",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "bootstrap": "cd backend && node scripts/bootstrap-admins.js",
    "lint": "cd frontend && npm run lint && cd ../backend && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
